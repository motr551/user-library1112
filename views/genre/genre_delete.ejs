
<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../layout/head.ejs'); %>
  <%- include('../layout/form.ejs'); %>
</head>
<body>
  <%- include('../layout/bodyMenu.ejs'); %>
  <div style="width: 850px; float:left; " > 
        
    <h1><%= title %>: <%= genre.name  %></h1>

    
    <% if ( genres_books.length> 0) { %>
      
      <p><strong>Delete the following books before deleting this genre</strong></p> 

      <div style="margin-left: 20px; margin-top:20px">

        <h4>Books:</h4>

        <dl>
          <% for (i=0; i<genres_books.length; i++) { %>
            
            <dt>
              <a href="<%= genres_books[i].url %>">
                <%= genres_books[i].title %>
              </a>
            </dt>
            <dd>
              <%= genres_books[i].summary %>
            </dd>
          <% } %>


        </dl>

      </div>

    <% } else { %>
      <p style="color:red" >Do you really want to delete this genre?</p>

      <form action="" method="POST">
        <div>
          <input type="hidden", name='genreid', required='true' 
          value=<%= genre._id %> >
        </div>

        <button style="color:red" type="submit">Delete</button>
      </form>
    <% } %>
   
     

  </div> 

</body>
</html>
