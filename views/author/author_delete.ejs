
<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../layout/head.ejs'); %>
  <%- include('../layout/form.ejs'); %>
</head>
<body>
  <%- include('../layout/bodyMenu.ejs'); %>
  <div style="width: 850px; float:left; " > 
        
    <h1><%= title %><%= author.name  %></h1>

    <p><%= author.dateOfBirth  %> - <%= author.dateOfDeath  %></p>

    <% if ( author_books.length) { %>
      <p><strong>Delete the following books before deleting this author</strong></p> 

      <div style="margin-left: 20px; margin-top:20px">

        <h4>Books</h4>

        <dl>
          <% for (i=0; i<author_books.length; i++) { %>
            <dt>
              <a href="<%= author_books[i].url %>">
                <%= author_books[i].title %>
              </a>
            </dt>
            <dd>
              <%= author_books[i].summary %>
            </dd>
          <% } %>


        </dl>

      </div>

    <% } else { %>
      <p>Do you really want to delete this author?</p>

      <form action="" method="POST">
        <div>
          <input type="hidden", name='authorid', required='true' 
          value=<%= author._id %> >
        </div>

        <button type="submit">Delete</button>
      </form>
    <% } %>
   
     

  </div> 

</body>
</html>
